// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "api_key",
    authDomain: "loginapp-31b15.firebaseapp.com",
    databaseURL: "https://loginapp-31b15.firebaseio.com",
    projectId: "loginapp-31b15",
    storageBucket: "loginapp-31b15.appspot.com",
    messagingSenderId: "499675766771",
    appId: "1:499675766771:web:197d34349d38450d373c86"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
